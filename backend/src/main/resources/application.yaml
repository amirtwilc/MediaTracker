spring:
  application:
    name: media-tracker
  datasource:
    platform: postgresql
    url: jdbc:postgresql://localhost:5432/media_tracker
    username: postgres
    password: mysecretpassword
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        jdbc:
          batch_size: 20
    open-in-view: false

  batch:
    jdbc:
      initialize-schema: always
    job:
      enabled: false  # Don't auto-start jobs
    params:
      chunk-size: 10
      skip-limit: 100

  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.LongSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      acks: all
      retries: 3
    consumer:
      key-deserializer: org.apache.kafka.common.serialization.LongDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      group-id: rating-notification-group
      auto-offset-reset: earliest
      enable-auto-commit: true
      properties:
        spring.json.trusted.packages: "*"

  graphql:
    graphiql:
      enabled: true
      path: /graphiql
    path: /graphql

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
      enabled: true

server:
  port: 8080
  error:
    include-message: always
    include-stacktrace: on_param

# JWT Configuration
jwt:
  secret: 6c607c38214901e634c42846c9e5ce576415e6ac9d4ca206a8a56bcc0d10fc966c607c38214901e634c42846c9e5ce576415e6ac9d4ca206a8a56bcc0d10fc96 #your-512-bit-secret-key-here-change-in-production
  expiration: 86400000  # 24 hours in milliseconds

# Logging
logging:
  level:
    org.springframework.graphql: DEBUG
    com.amir.mediatracker: DEBUG
    org.springframework.batch: INFO
    org.springframework.kafka: INFO
    org.hibernate.SQL: INFO

